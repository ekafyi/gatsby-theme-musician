/**
 * Configure your Gatsby site with this file.
 *
 * See: https://www.gatsbyjs.org/docs/gatsby-config/
 */

module.exports = {
  plugins: [
    {
      resolve: "gatsby-theme-musician",
      options: {
        // basePath: "/" // default is `/`
        // contentPath: "content" // default is `content`
      },
    },
  ],
}
