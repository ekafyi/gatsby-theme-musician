export default {
  container: "60rem",
  navMenuXSpacing: "0.75rem",
  navbarHeight: 48,
  // layoutTopMargin: ["2rem", "3rem"], // used for banner negative margin

  /**
   * Note: Values in theme scales are not responsive. We cannot use
   * the value below with theme-ui styling; instead, we can import
   * and use it as regular JS object.
   *
   * https://github.com/system-ui/theme-ui/issues/664
   */
  headerHeight: [48, null, 60], // NOT USED
}
